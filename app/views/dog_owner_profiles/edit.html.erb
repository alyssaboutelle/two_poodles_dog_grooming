<div class="row justify-content-center mb-3">
  <div class="col-md-8">
    <div class="card">
      <h2 class="card-header h4">
        Edit Dog Owner Profile #<%= @dog_owner_profile.id %>
      </h2>

      <div class="card-body">
        <%= render "form", dog_owner_profile: @dog_owner_profile %>

        <%= link_to :back, class: "btn btn-block btn-outline-secondary" do %>
          <i class="fas fa-chevron-left fa-fw"></i>
        <% end %>
      </div>
    </div>
  </div>
</div>
